// Academy-family pages: shipped selectors + threshold, now through the shared
// helper in /js/site.js (R2). Loaded via the shell's $scripts, after site.js.
sjReveal('.back-bar-reveal,.infra-new-reveal', 150, true);
