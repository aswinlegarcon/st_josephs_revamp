# PHASES.md — Master Implementation Plan: St. Joseph's MHSS Future-Proof CMS

> **This is the roadmap.** Companion documents: `WEBSITE_CONTEXT.md` (canonical site inventory — update when pages change), `SECURITY.md` (normative security catalog — its §4 checklist is mandatory for every phase, enforced by `CLAUDE.md`), `ADMIN_UI_DESIGN.md` (admin UI spec + image-generation briefs), `DYNAMIC_MIGRATION_PLAN.md` / `FEATURES_PLAN.md` (legacy reference for schema, pipeline, seed inventory — their "locked decisions" are superseded by §0 below).

---

## 0. Locked decisions (supersede all earlier docs)

1. **Backend:** modern structured PHP 8.3 — Composer PSR-4 (`SJ\` → `src/`), thin layers (controllers / repositories / services), PDO. **No framework.** Evolve the existing working engine (registry, media pipeline, schema, admin panel); never discard it.
2. **Admin:** hybrid — a professional standalone dashboard (extends the existing `sj-` panel) **plus** an on-page WYSIWYG overlay for live editing.
3. **Public revamp:** same look, clean code — one self-hosted Bootstrap 5.3.3, valid single-document HTML via a layout system, consolidated CSS with `:root` design tokens, all 15 documented bugs (WEBSITE_CONTEXT §10) fixed.
4. **Hosting:** MilesWeb shared hosting (mPanel, 10 GB, no cPanel/SSH). Deploy = file upload; `vendor/` committed; Docker is dev-only. The site is currently reported "48% healthy, very slow" → the performance track (F0–F4) is first-class work, not polish.
5. **Users:** single admin account now; schema and registry are multi-user-ready (`role` column lands in S2; per-entity permission flags come with roles later).

---

## 1. Target Architecture

**Principle: evolve, don't rewrite.** The working engine (registry concept, GD media pipeline, 24-table schema, admin panel + sj- design system) stays. What changes is *packaging*: PSR-4 namespaces, secrets out of the webroot, a front controller for the admin API, and a real layout system for public pages. Everything runs on stock PHP 8.3 + GD + PDO — no server-side build step; `vendor/` is committed.

**Directory layout** (repo root maps to the hosting account home; only `public_html/` is web-served):

```
composer.json                  # PSR-4: "SJ\\" => "src/"
config/
  config.php                   # real secrets — gitignored, NEVER committed (canonical location)
  config.sample.php            # committed template
src/
  Core/    Db.php, Config.php                    ← _libs/db.php, config.php loader
  Content/ Registry.php, Sanitizer.php,          ← _libs/registry.php, sanitize.php
           Repo/HomeRepo.php … Repo/GalleryRepo.php   ← _libs/repo.php split per area
  Media/   Pipeline.php, Html.php                ← _libs/media.php (GD pipeline; img_tag/bg_style)
  Admin/   Auth.php, Csrf.php, Audit.php,        ← _libs/edit.php (auth half) + new audit helper
           Controllers/{Field,Item,Order,Upload,Images,Link}Controller.php  ← admin/api/*.php bodies
  View/    Layout.php, EditAttrs.php             ← _libs/load.php + edit.php (data-edit-* emitters)
  helpers.php                  # e(), img_tag(), bg_style() — composer "files" autoload
views/
  layout.php                   # ONE <!DOCTYPE>/<head>/<body> master document
  partials/  navbar, footer, preloader, jumbotron, scroll-up, contact …
  pages/     home.php, about.php, section.php, academy.php, album.php …
vendor/                        # committed
database/                      # schema.sql, seed.php, migrations/, backfill.php — dev-only, never uploaded
public_html/
  bootstrap.php                # require ../vendor/autoload.php + ../config/config.php
  index.php, about.php, …      # same URLs; each becomes a ~15-line thin controller
  admin/  index.php, login.php, section.php, media.php, password.php
  admin/api/index.php          # single front controller: ?r=field.save|item.create|link.reorder…
                               #   (query routing — no rewrite dependency on mPanel)
  assets/vendor/bootstrap-5.3.3/   # the ONE self-hosted Bootstrap; css/tokens.css with :root vars
```

**Migration mechanics:** during P1–P4, each `_libs/*.php` becomes a 2-line shim (`require bootstrap; thin function wrappers`) so all 42 pages keep working mid-restructure; shims are deleted in R2. Admin API endpoint files (`field.php` etc.) likewise become forwards to the front controller until the overlay ships, then are removed. Local Docker mounts the repo root so the "one level above webroot" layout is identical in dev and prod.

**Stays:** schema.sql (plus additive migrations), registry-driven generic admin/API, media pipeline behavior, admin panel UI, all public URLs, the visual design.
**Changes:** flat `_libs/` → namespaced `src/`; committed demo creds → gitignored `config/config.php` above webroot; nested-document templates → single-document layout; 4 CDN Bootstraps → 1 self-hosted; `?v=time()` → static `SJ_ASSET_VER` constant.

---

## 2. Phase Breakdown (execution order)

Rules for every phase: it is small (0.5–2 days), independently shippable, leaves the site fully working, and closes with the `SECURITY.md` §4 checklist affirmed. Risk: L = low, M = medium, H = high.

### Stage A — Security first (Seq 1–5)

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 1 | ✅ S1 | Secrets out of repo | Move config to `config/config.php` **above the webroot** (the single canonical secrets location); gitignore it; commit `config.sample.php`; rotate DB creds. **Verify mPanel gives a writable home dir above `public_html`** (if not: fall back to `public_html/../` equivalent per mPanel layout and record the result here). | `config/config.php` (new, ignored), `config.sample.php`, `_libs/config.php`→loader, `.gitignore`, `docker-compose.yml` | — | `git grep` finds no credentials; site boots from `config/config.php`; sample committed; mPanel layout confirmed in writing. | L |
| 2 | ✅ S2 | Kill default admin | Migration adds `role`, `must_change_password`, `password_changed_at` to `admin_users`; new `admin/password.php`; forced change on first login; prod seeder generates a random password printed once to CLI (never `admin123`). | `database/migrations/001.sql`, `database/seed.php`, `admin/password.php`, `admin/_layout.php`, `admin/login.php` | S1 | Fresh seed → login → forced to set a strong password before any admin page or API works; `role` column exists (multi-user ready); `admin123` appears in nothing deployable. Covers SEC-08. | L |
| 3 | ✅ S3 | Session hardening | Idle timeout 30 min, absolute 12 h, cookie `Secure/HttpOnly/SameSite=Lax`, `session_regenerate_id` on login and periodically; private `session.save_path` on prod. Fix login lockout counter persistence + generic error message (SEC-06). | `_libs/edit.php` (session half), `admin/login.php` | S2 | Idle 31 min → API returns 401 JSON, panel redirects to login; cookie flags visible in devtools; 20-wrong-password script shows persistent lockout and byte-identical error responses. Covers SEC-06/07. | L |
| 4 | ✅ S4 | .htaccess hardening + security headers + audit log | Root `public_html/.htaccess`: deny dotfiles (`.git`, `.env`), deny `\.(sql|bak|old|zip|tar|gz|log|md|lock|swp)$`, `Options -Indexes`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`. Admin pages/API emit `X-Frame-Options: DENY` + CSP. New `audit_log` table + `SJ\Admin\Audit::log()` called from login success/failure and every mutating endpoint. `logout.php` → POST + CSRF. | `public_html/.htaccess` (new), `admin/api/_bootstrap.php`, `admin/_layout.php`, `admin/login.php`, `admin/logout.php`, `database/migrations/002.sql` | S3 | `curl -sI` shows the right header set per context; admin refuses to iframe; `/.git/HEAD`, `/config.php.bak` style probes → 404/403; every login attempt + mutation writes an audit row. Covers SEC-05/12/13/14/15/19/20. | L |
| 5 | ✅ F0 | Performance baseline | Capture the "before" numbers so F1–F4 improvements are provable: mobile Lighthouse + WebPageTest on 6 representative pages (Home, about, infrastructure, one academy, gallery hub, one album); record MilesWeb health score. Adopt the **query budget rule**: every converted page ≤ 12 SQL queries; `settings` cached per request. | `docs/perf-baseline.md` (new) | — | Baseline table committed (scores, payload sizes, request counts, TTFB); budget rule written into CLAUDE.md conventions. | L |

### Stage B — Platform restructure (Seq 6–9)

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 6 | ✅ P1 | Composer skeleton | `composer.json`, autoload, `SJ\Core\{Db,Config}`; `_libs/db.php` + `config.php` become shims. | `composer.json`, `src/Core/*`, `public_html/bootstrap.php`, shims | S1 | All pages + admin work unchanged; `vendor/` committed; classes autoload. | L |
| 7 | ✅ P2* | Port libraries | media/sanitize/registry/edit → `SJ\Media`, `SJ\Content`, `SJ\Admin\{Auth,Csrf}`, `src/helpers.php`; shims kept. | `src/Media/*`, `src/Content/*`, `src/Admin/*`, shims | P1 | Upload→renditions works; Home renders identically; admin login works. | M |
| 8 | ✅ P3 | Admin API front controller | `admin/api/index.php?r=…` router → `SJ\Admin\Controllers\*`; old endpoints forward. | `admin/api/index.php`, `src/Admin/Controllers/*`, `admin/assets/panel.js` (base URL) | P2 | Every existing panel operation (field save, item CRUD, reorder, upload) works through the router; forwards return identical JSON. | M |
| 9 | ✅ P4* | Public layout system | `views/layout.php` single document + `SJ\View\Layout::render()`; convert **Home only** as proof; parametrized partials for navbar/footer/preloader. | `views/layout.php`, `views/partials/*`, `views/pages/home.php`, `index.php` | P2 | Home output is valid single-document HTML (W3C validator: no nested doctypes), pixel-identical visually. | M |

> **P4\* executed scope:** the layout **engine** (`SJ\View\Layout::render()` with a hardcoded page whitelist), the `views/` structure (`views/layout.php` shell + `views/pages/home.php`), and the thin-controller `index.php` are delivered — Home renders byte-identically (verified: only cosmetic whitespace + a correctly entity-escaped `<title>` differ) and the other 41 pages are untouched. The shared fragments still come via `get_templates()`, so Home still contains their nested `<head>`/`<body>` (12 doctypes). Eliminating those into a true single document is **coupled to R1a's Bootstrap unification** (both rewrite the same asset heads) and is done there per-family, not duplicated here.

> **P2\* executed scope:** the clean, security-critical libraries are ported to classes now — `SJ\Content\Sanitizer` (XSS gate), `SJ\Content\Registry` (SQL-safety keystone), `SJ\Admin\Audit` — each with a delegating `_libs` shim. The larger, battle-tested and entangled units (the media GD pipeline, `repo.php`, and the just-hardened `edit.php` session/auth/csrf/emitters + `src/helpers.php`) are intentionally **deferred to R2** to avoid churning verified security/upload code; nothing in P3/P4 depends on them being classes.

### Stage C — Public shell, per family (Seq 10–12)

Self-hosted Bootstrap 5.3.3 + `css/tokens.css` (`:root` navy/gold/font vars) land in R1a and are reused by R1b/R1c. Inner page markup is untouched in this stage — only the document shell changes.

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 10 | ✅ R1a | Layout wrap — hub pages | Wrap about, staffs, academics, achievements, co-curriculum, sports, infrastructure, gallery in the layout; install `assets/vendor/bootstrap-5.3.3/` + `tokens.css`; fix dead `curriculum.php` navbar link (bug 5) and duplicate `</body></html>` (bug 10 part). | 8 page controllers + `views/pages/*`, `assets/vendor/bootstrap-5.3.3/`, `css/tokens.css`, navbar partial | P4 | These 8 pages: zero CDN Bootstrap requests, single-document HTML, visual diff = none, dropdowns work. | M |
| 11 | ✅ R1b | Layout wrap — academies + sections | Wrapped the 18 academy-family pages (shared `css/academy.css` + `js/academy.js`, extracted verbatim from the identical per-page inline block; per-page `.bg-1` only) and the 4 section pages (kg/primary/highschl/highsec — own CSS, hero + `.infra-new` carousels + accordion translated, jumbotron via shell). | `css/academy.css`, `js/academy.js`, 22 controllers + `views/pages/*` | R1a | **Done:** all 22 single-document, no CDN Bootstrap/jQuery/BS4 dialect, 200; text + `<br>`/`<span>`/`<p>`/slide/img counts identical to baseline `6bd0d11`; tamilacademy computed-styles match baseline; carousels/accordion run under BS5. | M |
| 12 | ✅ R1c | Layout wrap — gallery family | Wrapped gallery's 11 `gal-*` pages (already BS5 + custom lightbox — pure shell-wrap, inline style/script kept verbatim, redundant Popper+BS5 CDN dropped; `gal-sciexpo`→`gal-spach` class quirk preserved). Site-wide sweep: every page except `index.php` is CDN/jQuery-free. | 11 controllers + `views/pages/gal-*` | R1a | **Done:** all 11 single-document, no CDN/get_templates leak, 200; `<img>` src list identical to baseline; lightbox verified live. Site-wide: only `index.php` (home) still carries jQuery/BS4 — its shared content templates convert in a later home-unification pass. | M |
| 12b | ✅ R1d | Home unification | Converted the 8 shared content-section templates to clean parametrized partials (`views/partials/`: carousel, card, update-scroll, new-updates, marks-scroll, testimonial, contact, groups — all queries moved to controllers); home + highsec now render fully in the BS5 shell; deleted the legacy `views/layout.php`; killed the last jQuery/BS4 CDN. **`css/home-bs4-remnants.css`** (linked LAST in home's body, mirroring the baseline's final stylesheet — the footer template's Bootstrap 4.3.1) re-supplies exactly the BS4 fragments that slot contributed (`.card-deck`, `.btn`/`.btn-lg` radii/sizes, `.card` border/radius on testimonial cards, `.container` 15px gutters, BS4 link colors), so the render is pixel-faithful. Keyboard hero-nav rewritten jQuery→BS5 API. Query budget: batched `mark_entries` IN-query + profile image JOIN → home 15→**12** queries (at budget), highsec 5→3. | 8 partials, `views/pages/{home,highsec}.php`, `index.php`, `highsec.php`, `Layout.php`, `css/home-bs4-remnants.css`, `_libs/repo.php` | R1a–c | **Done:** all 42 pages single-document, zero CDN Bootstrap/jQuery site-wide; home visible text/img/structure identical to baseline; 16 computed-style probes match baseline exactly (incl. the 3 cascade-tie traps); hero keyboard nav + both carousels + EmailJS/reCAPTCHA/ionicons live; 0 console errors; highsec byte-faithful (3 doctypes→1). | M |

### Stage D — Fast, deployable shell (Seq 13–14)

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 13 | ✅ F1 | Delivery basics | `.htaccess` `mod_deflate` + `mod_expires`/`Cache-Control` (images + versioned css/js `max-age=31536000, immutable`; HTML `no-cache`); kill `?v=time()` → `SJ_ASSET_VER` constant bumped per deploy; defer non-critical JS; verify OPcache. | `public_html/.htaccess`, layout/partials asset tags | R1a–c | Repeat page view makes 0 asset re-downloads; PageSpeed TTFB < 0.8 s; text assets served gzipped. | L |
| 14 | ✅ X1 | Deploy runbook + first prod ship | `DEPLOY.md` (release-zip whitelist: `public_html/ src/ views/ vendor/ config/config.sample.php`; never-upload list: `database/ .git *.md docker files dev config`); mPanel DB provisioning; `admin/health.php` smoke-check (DB, GD, writable media/, PHP ver, OPcache, last backup age) behind a secret token; HTTPS + free SSL enforced in mPanel; file perms 755/644, config 600. **First production deployment happens here** — a secure, restructured, faster shell with Home dynamic. Ship to prod after every phase from now on. | `DEPLOY.md`, `admin/health.php` | S1–S4, P1–P4, R1a–c, F1 | Fresh mPanel deploy following the runbook alone passes health check; rollback = re-extract previous zip; SEC-12/20/22 probes pass on prod. | M |

### Stage E — Content areas end-to-end (Seq 15–27)

Each phase = migration/seed + repo class + registry entries + public render from DB + admin panel section, so the area is **fully editable when the phase closes**. Photo tooling (M1–M3) is interleaved exactly before the photo-heavy areas need it.

| Seq | ID | Name | Goal / bugs fixed | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 15 | ✅ C1 | Settings, contact, footer, jumbotron | `settings`-driven site-wide strings; fixes email mismatch (bug 12), ticker typo (bug 14 part). **Contact-form abuse hardening (SEC-23):** replace client-side EmailJS send with a rate-limited server-side endpoint (or, minimum: EmailJS domain-allowlist + key rotation) + honeypot field; reCAPTCHA kept. | seed.php, `Repo/SettingsRepo`, Registry, `views/partials/{contact,footer,jumbotron}`, `js/contact.js`, admin section | X1 | Change phone/email in admin → footer+contact update; mailto matches displayed address; contact submits ≤ 5/hour/IP; EmailJS private usage not spammable from other origins. **Done:** 14 whitelisted settings seeded byte-identical + editable via a dedicated `?r=settings` endpoint (settings stays OUT of the registry) with per-key validation, CSRF, audit; footer/contact/jumbotron render via `e()` from settings; bug 12 fixed by construction (mailto = displayed setting). SEC-23: EmailJS SDK + public key removed; form posts to `/api/contact.php` (POST-only, origin-pinned, honeypot, server-side reCAPTCHA when configured, 5/hour/IP rate limit via `contact_submissions`, enquiries always stored, optional `mail()` relay). Verified: admin edit → both surfaces update; 405/403/400/429 paths exercised; visible text identical to baseline; seeder idempotent. **Ticker typo (bug 14 part) intentionally deferred to R3** — visible-text changes are frozen. | L |
| 16 | ✅ C2 | About page | `pages`+`profiles` (president/principal messages, history, rules, timings, diary file); fixes "Infrastrucutre" typo (bug 14 part). | seed, `Repo/ProfilesRepo`, `views/pages/about.php`, `about.php`, Registry, admin | C1 | Edit principal message in admin → about.php AND home update (shared row); sanitizer allows only the whitelist. **Done:** `pages` row 'about' + 3 page-scoped hero_slides + profiles rows president/history/rules seeded byte-identical (principal row shared with home by design); view renders all four blocks + carousel from DB with `e()`/ed_* emitters; timings table + diary download stay structural. Admin: new 'About Page' section (slides list + generic profile row editors). Verified: visible text identical pre/post; principal edit shows on BOTH pages; sanitizer strips `<script>`/`onclick` (kept whitelist only); seeder idempotent; 9 queries (≤12). **"Infrastrucutre" typo (bug 14 part) deferred to R3** — visible text frozen. | L |
| 17 | ✅ C3 | Staffs + testimonials | `profiles` rows for staff blocks + home testimonials become editable lists. | seed, ProfilesRepo, `views/pages/staffs.php`, home partial, admin (screen 16 of ADMIN_UI_DESIGN) | C2 | Add/edit/delete/reorder a testimonial end-to-end. **Done:** new `testimonials` table (migration 004; `name_html` is a sanitized rich field because the shipped headings embed `<br>`) + registry entity; testimonial partial + staffs page render from DB (pages row 'staffs' + 2 hero slides + profiles staff_love/staff_team/staff_tour, exact shipped copy); admin gets Staffs Page + Testimonials sections (full list-manager). `repo_hero_slides` now JOINs its images (one query) so home stays at 12 queries with the extra list. Verified: create→reorder→edit→delete round-trip via the panel API renders correctly on home at each step; home visible text byte-identical to baseline before/after; staffs text fragment-identical to the static version; seeder idempotent; budgets home 12 / staffs 6 / about 7. | L |
| 18 | ✅ M1 | Link API | `LinkController` (`link.list/attach/detach/reorder`, transactional) + collection repo helpers for `image_links`. | `src/Admin/Controllers/LinkController.php`, router, repos | P3 | curl-level: attach/detach/reorder persists with correct sort order; CSRF + registry-whitelisted owner_types enforced. **Done:** `admin/api/link.php` (`?r=link`, actions list/attach/detach/reorder) — owner table resolves ONLY via new `Registry::ownerTypes()` (page/section/academy/facility/album/album_year); owner + image rows must exist; attach appends transactionally with MAX+1 position (duplicate → friendly uq_member error); reorder is transactional and refuses ids outside the collection; every mutation audited. Public read path: `repo_linked_images()` (one JOIN query). Verified via 11-case API matrix incl. whitelist bypass, cross-collection reorder and missing-CSRF (all rejected). | L |
| 19 | ✅ M2 | Manage-photos UI | Panel "Manage photos (n)" modal: grid of linked images, add-from-library / upload, drag reorder, detach; wired to hero carousel + update slides first. | `admin/assets/panel.js`, `panel.css`, `admin/section.php` | M1 | Reorder hero slides by drag → Home carousel order changes on reload. **Done:** (1) every orderable panel list now supports drag-reorder (rows draggable; drop → one order API call) alongside the ↑/↓ buttons; (2) the reusable "Manage photos" modal (`panel_photos_attr()` → `data-panel-photos` → tile grid over the M1 link API) with add-from-library/upload via the existing picker, tile drag-reorder and remove — C4–C9 sections consume it. `SJ_ASSET_VER` bumped (immutable-cache rule). Verified live: hero slide dragged below #2 → DB order 2,1,3… → home carousel leads with the new first slide on reload → restored; modal round-trip on a test collection (attach ×2 from library, tile drag persists order, detach) — 0 console errors. | M |
| 20 | ✅ C4 | School sections family | kg/primary/highschl/highsec on ONE template: `school_sections`, `timeline_entries`, `section_events`; inner carousels via image_links; fixes "Higer Secondary" (bug 14 part). | seed, `Repo/SectionsRepo`, `views/pages/section.php`, 4 thin controllers, admin | C1, M2 | All 4 URLs render from DB; add a timeline entry to KG in admin → appears; groups/marks-scroll still on highsec. **Done:** content extracted byte-faithfully from the static views by `database/extract-sections.php` → `seed-data/sections.php` (committed); migration 005 adds `events_heading` (KG says 'Events of', the rest 'Exams and Events of'); ONE `views/pages/section.php` template + 4 thin controllers (static view files deleted); inner carousels are the first `image_links` consumers (owner 'section' via M1/M2); registry + admin 'School Sections' screen (4 tabs: hero slides, intro/grade-card row, Manage-carousel-photos button, timeline + events list-managers, multiline textarea support added for events_text). Verified: all 4 pages visible-text/img/slide-count identical to pre-C4 (highsec incl. groups+marks, 72 slides); accordion + both carousels live under BS5; timeline add→appears (with <br> line rendering)→delete; intro edit→live→restore; budgets kg/primary/highschl 7, highsec 9; seeder idempotent. | M |
| 21 | ✅ C5 | Academies family | 18 pages (15 academies + band/ncc/artandexpo) on one template from `academies` + carousel links; fixes .jpg/.jpeg 404s (bugs 1, 2) and "Creativness" (bug 14 part). | seed, `Repo/AcademiesRepo`, `views/pages/academy.php`, 18 thin controllers, co-curriculum cards, admin | C4 | Every academy URL renders; Art-Expo images load; edit write-up in admin → live; co-curriculum grid comes from the same rows. **Done:** `extract-academies.php` → committed `seed-data/academies.php` (18 rows byte-faithful; the six broken `.jpg`→`.jpeg` references resolved to the real files — bugs 1/2 FIXED, Art-Expo/abacus photos now load); ONE `views/pages/academy.php` + 18 thin controllers (18 static views deleted); carousels are image_links (owner 'academy'); co-curriculum grid loops the same rows (reveal-suffix sequence incl. the shipped duplicate 'reveal4' quirk preserved; hide-toggle drops the card). Admin 'Academies' screen: reorder/edit/toggle + per-academy M2 photos button. Verified: 18/18 pages contain their static text verbatim; only image deltas = the six fixes (all 200); co-curriculum text identical; write-up edit → live → restored; hide → 17 cards → restore 18; budgets: academy 3, co-curriculum 2. **"Creativness" typo (bug 14 part) deferred to R3** — visible text frozen. | M |
| 22 | ✅ C6 | Sports | `sports` table; unique accordion ids (bug 11 part). | seed, `Repo/SportsRepo`, `views/pages/sports.php`, admin | C1 | 9 sports editable; each accordion expands independently. **Done:** `extract-sports.php` → `seed-data/sports.php`; pages row 'sports' + 3 hero slides + 9 sports rows seeded byte-faithful; DB-driven view keeps accordion-1..9 (bug-11 fix) + collapseOne..Nine word-form ids + the reveal-suffix sequence; admin 'Sports' screen (hero + sport-card list-managers). Verified: visible text identical (3158 chars), img basenames order-identical (relative→absolute path normalization only), accordion/collapse id sets identical; accordions 0 and 4 opened independently; 4 queries; seeder idempotent. | L |
| 23 | ✅ M3 | Crop UI | Vendored Cropper.js on upload/replace, locked to preset aspect; server re-renders renditions from stored `crop_rect`; "Re-crop" reopens with the same rect and bumps `?v=`. | panel.js, `UploadController`, `Media/Pipeline`, `admin/assets/cropper/` | M2 | Upload portrait photo into 16:9 slot → choose crop → rendition matches selection, no distortion; re-crop changes the URL version. **Done:** Cropper.js 1.6.2 vendored (self-hosted, `admin/assets/cropper/`); upload modal shows a crop stage locked to the slot aspect (cover presets; fit presets skip it) and sends `crop_rect`; media-library tiles get ✂️ Re-crop (uploads only) → modal primed with the stored rect → new `?r=recrop` endpoint regenerates renditions from the preserved original, updates crop_rect and bumps `version` (URL changes). Preset metadata now flows via a CSP-safe `data-sj-presets` body attribute — this also fixes the previously CSP-blocked inline `SJ_PRESETS` script. Verified with a red/blue half-image: chosen bottom-half crop → blue 800×450 rendition; re-crop to top → red rendition, version 1→2, thumb `?v=2`; audit row written. | M |
| 24 | ✅ C7 | Infrastructure | `facilities` (15 showcases, admin can add more); fixes anchor off-by-one (bug 6), malformed Conference-Hall carousel + double bundle (bug 10 part), duplicate carousel ids (bug 11 part), missing infrastructure.js ref (bug 4). | seed, `Repo/FacilitiesRepo`, `views/pages/infrastructure.php`, admin | C1, M2 | Nav button N scrolls to facility N; 15 carousels independent; no 404s in console; new facility added in admin appears with nav anchor. **Done:** `extract-infrastructure.php` → `seed-data/facilities.php` (source-SLICE parsing — the shipped markup has unclosed divs that DOM repair mangles); 15 facilities + hero seeded; carousels are image_links (owner 'facility', batched 2-query repo — no N+1); the `.bg-N` background rules moved from infrastructure.css into the view, emitted per facility from its DB photo with the identical declarations (admin bg changes now apply; new facilities get one automatically). Registry facility entity incl. `slug`; admin Infrastructure screen (hero + facility list w/ M2 photo managers). Verified: visible text byte-identical (difflib opcode-level), img order identical, anchors #bg-1..15 + 15 unique carousel ids; ACCEPTANCE LIVE: created facility 16 via panel → nav anchor + section + bg style all rendered → deleted (orphan link cleaned). 5 queries. Known reconciliation: facility-15's shipped 2-indicators-for-3-slides mismatch now renders 3 dots (indicators derive from slides). | M |
| 25 | ✅ C8 | Achievements + academics | `achievements` zig-zag + certificates (two typed loops); academics 4 grade cards from `school_sections`; fixes schname.JPG case (bug 9), "Sports Achivements" (bug 14 part). | seed, `Repo/AchievementsRepo`, views, admin | C4 | Add an achievement in admin → renders in correct zig-zag alternation; grade cards match section data. **Done:** `extract-achievements.php` → 3 hero slides + 10 achievements + 4 awards seeded byte-faithful (typed lists, per-type positions); view renders both lists via one paired zig-zag helper (2-per container, second = reverse); academics grade cards loop `repo_sections()` (the C4 rows — one edit updates both pages); registry achievement entity (enum type); admin Achievements screen (hero + two typed list-managers). Verified: achievements + academics visible text byte-identical (after correcting an accidental jumbotron enable — caught by the diff); ACCEPTANCE LIVE: created an 11th achievement via the panel → rendered as the plain (img-left) first item of a new pair → deleted. Budgets 4/2. **bug 9 (schname.JPG case) turned out already-consistent in the shipped views; "Sports Achivements" typo deferred to R3.** | L |
| 26 | ✅ M4 | Media library page | `admin/media.php`: browse/search all images (uploads + 478 legacy), usage counts (where linked), alt-text edit, delete only when unlinked, orphan filter, detail drawer. | `admin/media.php`, `ImagesController` | M2 | Deleting a linked image is blocked with "used in X" message; orphans deletable; legacy photos searchable. **Done:** new `?r=image` endpoint (meta/usage/delete) — usage sweeps ALL 11 reference columns (image_links + every FK); delete refused with a per-place breakdown when used, otherwise removes renditions+row (+files for uploads; legacy files stay on disk and re-register on seed); alt-text editing; the images list now carries per-image usage counts via ONE UNION-ALL query + `filter=orphan`; media grid shows 🔗 used-badges, an 'Unused only' toggle, and a click-through detail modal (usage breakdown, description editor, guarded delete). Verified live: used image → usage '1 × hero slide', delete refused with that message, alt saved+restored; unused legacy image deleted cleanly (file untouched, re-registered by the seeder); orphan filter returns only used=0. | L |
| 27 | ✅ C9 | Gallery family | Hub + 11 albums on one template from `gallery_albums`/`album_years`/`image_links`; fixes gal-sciexpo orphan → 301 (bug 3), year-toggle mismatches (bug 7), lightbox default (bug 8), duplicate ids (bug 11 part). **Keyboard features (FEATURES_PLAN §3–4):** home hero ←/→; lightbox ←/→/Esc navigating only the active year, with typing/modifier/`sj-edit-mode` guards. | seed, `Repo/GalleryRepo`, `views/pages/{gallery,album}.php`, 11 stubs, gal-slider partial, carousel partial script, admin | M2, M4 | Year filter labels match shown photos on every album; attach a photo to an album+year in admin → grid + lightbox show it; keyboard nav works per FEATURES_PLAN §5 acceptance list. **Done:** `extract-gallery.php` → 10 albums / 14 years / 247 photos (image_links owner 'album_year'); per-album shipped CSS extracted verbatim to `css/albums/{slug}.css`; ONE `views/pages/album.php` + 10 controllers; hub cards loop `repo_albums()` (migration 006 adds `heading`+`card_sub` — hub title and page h1 are different shipped strings). **Bugs fixed per plan:** #3 gal-sciexpo (stale unlinked copy of gal-spach) → 301; #7 label `for=` now match inputs AND label text now matches the photo groupings (visible '2024'→'2022'/'2023' on 4 albums — the acceptance requires it); #8 popup default = the album's real first photo; #11 dead duplicate `image-N` ids removed. **Lightbox upgraded:** navigates ONLY the active year + ←/→/Esc with typing/modifier/edit-mode guards. Admin Gallery screen (album tabs → card row + years list + per-year M2 photo managers). Verified: text/rendered-img diffs zero everywhere except the four sanctioned fixes; keyboard cycled exactly the 21 active-year photos with 0 leaks into the other year; attach princ1.jpg → in grid → detach → gone; budgets album 4 / hub 3; seeder idempotent. | M |

### Stage F — Live-edit overlay (Seq 28–29)

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 28 | ✅ O1 | Admin bar + inline text | `views/partials/admin-bar.php` (rendered only when `is_admin()`), `js/admin.js`: activate `data-edit-*` — inline editing + Bold/Gold mini-toolbar, save via field API; edit-mode toggle is POST+CSRF; edit-mode pages send `X-Frame-Options: DENY`. | admin-bar partial, `public_html/js/admin.js`, `SJ\View\EditAttrs`, layout.php | P3, C1–C9 (progressively) | Logged-in admin on any converted page: click text → edit → save → persists; logged-out visitors get zero admin markup/JS bytes. **Done:** `admin/editmode.php` (POST+CSRF toggle, audited, same-site-path redirect only) + `views/partials/admin-bar.php` (fixed bottom bar, admins only) + `public_html/js/admin.js` / `css/admin.css` (sjov- namespace). Text fields → contenteditable=plaintext-only (Enter saves / Esc cancels / click-away saves); rich fields → floating B/I/Gold/✕fmt/Save toolbar (display:contents wrappers boxed during editing, server-sanitized value reflected back); enums → select popover. Edit-mode responses send X-Frame-Options: DENY (verified: present in edit mode, absent for visitors). Verified live: heading edited → persisted on reload → restored; Esc reverted a rich edit; visitor HTML contains zero sjov-/data-edit/admin.js bytes. | M |
| 29 | ✅ O2 | Overlay item + image ops | Add/delete/reorder items in place; click image → picker/crop modal (reuses panel modals, `sjov-` CSS namespace). | admin.js, shared modal JS extracted from panel.js | O1, M3 | Admin adds a topper card from the live Home page; swaps a hero image without opening the panel. **Done:** shared modal core extracted from panel.js to `admin/assets/sj-ui.js` (window.SJUI: modals, picker, forms, rich editor, API client) — panel.js now consumes it via aliases (panel regression-tested: edit modal, photos modal, drag, media all intact). Overlay: hover chips on `data-edit-item` (label + ↑↓ per-scope reorder + 🗑 delete), ＋Add buttons on `data-edit-add` containers (same registry-driven form modal as the panel, incl. multiline parity fix in `ed_add`), 📷 badges on `data-edit-img` slots → picker → field save. Verified live on home: topper added via the marks ＋ button → rendered; hero slide photo swapped via 📷 without the panel → restored; chip-delete removed the test topper. | M |

### Stage G — Revamp completion (Seq 30–31)

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 30 | ✅ R2 | CSS consolidation | Merge 20 CSS files → `tokens.css` + per-family sheets; delete dead rules/unlinked files (bug 13); one shared `reveal()` in `js/site.js`; delete `_libs` shims + `_templates/`; drop remaining jQuery once no BS4 dialect remains. | `css/*`, `js/site.js`, views | C1–C9 | No unused stylesheet requests; visual diff none; total CSS size ↓ ≥ 40%; `_libs/`+`_templates/` gone. **Done:** 34 css files → 15 (170,671 B → 98,495 B, **−42.3%**): the 4 cloned section sheets → one `sections.css` (union verified line-by-line; real deltas scoped by existing body classes; dead `.{slug}-carousel` clones dropped — the live wrapper is `.abt-carousel`/`.kg-carousel`; the shipped `position:{slug}solute` typo-quirks omitted with comments, render-identical), 10 album sheets → `album.css` (canonical `.album-container` added beside slug classes; deltas: annual 50px/font line, independence #333, 3× scroll-behavior), home's 4 sheets → `home.css` (concat, link order kept; `home-bs4-remnants.css` stays last-in-body), dead `navbar/new-updates/testimonial.css` deleted (bug 13 — requested by no page). JS: 23 reveal/blur copies (16 inline + 7 files) → `js/site.js` `sjReveal()`/`sjBlurCards()` with each page's shipped selectors/thresholds (50–150) preserved; `carousel.js`+`card.js` deleted. `_libs/` (9 files) ported to `SJ\Admin\Auth`+`Csrf`, `SJ\View\EditAttrs`, `SJ\Media\Pipeline`+`Html`, `SJ\Content\Repo`, counting-PDO into `SJ\Core\Db`; historic global names live in `src/helpers.php` (Composer files autoload); `bootstrap.php` replaced `load.php`; `_libs/` + `_templates/` (0 callers) **deleted**. jQuery: already absent; zero BS4 dialect. Verified: 52/52 computed-style+layout hashes (26 pages × 2 viewports, every element + ::before/::after) identical to pre-R2 baseline; all 41 pages 200; admin login/field-save/upload/recrop-delete round-trips green; seeder idempotent ×2; zero console errors; visitor overlay bytes still zero. | M |
| 31 | ✅ R3 | Validation + a11y sweep | Unique IDs everywhere (bug 11 done), remaining typos (bug 14 done), absolute `/photos/...` paths only (bug 15), alt text on all images, W3C-valid pages. | views, seed content | R2 | Validator: 0 errors on all 42 URLs; grep finds no duplicate ids, no `../photos`. **Done — Nu (W3C) validator: 368 errors → 0 across all 42 documents** (unique ids + alt presence are validator-enforced, so those pass by the same run). Bug 14: `Higher Secondary` ×3 hero captions, `Annual Day` (title + heading — the h1 renders identically via the shipped text-transform), `Sports Achievements`, `Creativeness`, and `carosel1.jpg`→`carousel1.jpg` (file renamed; FKs repointed) — fixed in seed-data AND applied to live DBs via idempotent REPLACE-fixups in seed.php (re-run = 0 rows); `Inaguration`/`Infrastrucutre` were already correct. Bug 15: all 12 css `../photos`/bare `photos/` urls → site-absolute (resolution-identical). Bug 9 (real fix): achievements' `.achieve-container` background referenced `schname.JPG` (never existed) — the photo layer 404'd silently on the baseline; corrected to `.jpg`, the photo now renders under the gradient (dead-link repair, flagged). Validation mechanics, all pixel-verified: the 12 body `<style>` blocks became `/css/partials/*.css` body **links** (valid HTML, same document position ⇒ same cascade) with the 2 DB-driven blocks (academy/infrastructure) emitted as head styles via the shell's new `$sjHeadCss`; the famous invalid `.navbar background: linear(…)` and `background-color: none` quirks are preserved as comments (browser ignored them — render-identical, flagged per CLAUDE.md); dropdown/accordion `aria-labelledby` on plain divs removed; album radios' `autocomplete` removed; `tel:` href normalized; buttons/labels inside anchors → `.btn` spans (computed-identical); heading-level skips fixed by re-leveling (footer h4→h2, testimonial h3→h2, motto/What's-Unique h3→h2, New-Updates h5→h3 + slide h5→h4, sports accordion h5→h4) with Bootstrap's RFS sizes pinned exactly (calc + ≥1200px caps); the shipped unclosed `.update-carousel` div closed exactly where the parser recovered it; the diary `span>p` unwrapped (color moved to the p). Verified: 52-capture computed-style+rect harness — every page identical to baseline except the enumerated sanctioned deltas (typos' text, achievements bg, style-only `appearance`/wrapper-font-size props with rects identical to 0.1px) plus the owner's own live ticker reorder made through the O2 overlay mid-session (audit log id 89). Zero console errors; seeder idempotent. | L |

### Stage H — Performance, SEO, ops (Seq 32–36)

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 32 | ✅ F2 | Legacy rendition backfill | Batch-generate WebP/JPEG renditions for all 478 legacy photos **in Docker** (`database/backfill.php`) — never on the shared host; upload the `media/` output + SQL delta; route all page images through `img_tag()` (`<picture>`, `srcset`, lazy loading below the fold, explicit width/height). | `database/backfill.php`, views | C1–C9, X1 | No >300 KB original photo served on any page; Home image payload ↓ ≥ 70% vs F0 baseline; zero image-driven CLS. **Done:** `backfill.php` generates the exact (image, preset) pairs the pages request from the FK/link map — 494 rendition sets (JPEG+WebP, 988 rows) + 10 optimized `media/static/` copies of the CSS-referenced design assets (testimonial PNGs 920 KB→78 KB etc.); idempotent; renditions()/repo image JOINs keep every page ≤12 queries. **Legacy geometry contract (every clause measured with a per-element layout verifier against a properly force-loaded pre-F2 baseline):** fit-mode renditions (downscale only — cover-crops moved ratio-driven slots), served as **plain `<img>`** (the `<picture>` wrapper became the flex item in shipped rows and wrapped them) with **no width/height attrs** (attr heights pinned width-only slots); missing pair ⇒ original `/photos` file. Regeneration bumps `images.version` ⇒ URLs change ⇒ caches bust. Result: **16/16 pages, zero elements moved >2 px**; home images 9,834→1,108 KB (−89%), largest file 270 KB; M4's `SJ_IMAGE_REFS` gap fixed (section_events + gallery_albums refs were missing from the delete guard + orphan filter). | M |
| 33 | ✅ F3 | SEO pack | Unique `<title>` + meta description per page (columns on `pages`, editable in admin SEO screen), canonical + OG tags, generated `sitemap.xml`, `robots.txt` (`Disallow: /admin/`). | migrations, layout.php, `admin` SEO screen, sitemap script | C1 | Sitemap lists all public URLs; Lighthouse SEO ≥ 95; every page has a distinct title/description. **Done:** migration 007 `seo_meta` (slug-keyed — the 41 URLs span pages/academies/albums, one flat table covers all) + schema mirror; 41 hand-written titles+descriptions seeded (INSERT IGNORE — admin edits win); registry entity (title/description only, not creatable/deletable) + panel **SEO screen** (+ `canDelete` support in panel_row) + dashboard card; shell emits title/description/canonical/OG per URL (slug from SCRIPT_NAME — server-set; base_url from config); static `sitemap.xml` (41 URLs) + `robots.txt` (Disallow /admin/ /api/, Sitemap line). Verified: 41/41 distinct titles; Lighthouse SEO **100** on album/academy samples, 92 on the two pages whose only flag is the literal "Read more"/"Learn more" button copy (school-editable in Site Settings — documented). | L |
| 34 | ✅ F4 | Perf audit + Cloudflare | Lighthouse/WebPageTest re-run vs the F0 baseline; fix stragglers; optionally front with Cloudflare free (DNS move, `/admin*` bypass rule, Brotli). | — | F1–F3 | Mobile Lighthouse on Home/infrastructure/an album/an academy: **Perf ≥ 80, SEO ≥ 95, Best-Practices ≥ 90, A11y ≥ 90**; fully-loaded < 3 MB / < 25 requests on Home; MilesWeb health re-check documented. **Done (full results table in docs/perf-baseline.md §F4):** home 10,344→**1,906 KB** (−82%, <3 MB ✅), images −89% ✅, ≤12 queries ✅, CLS ≤0.05 everywhere ✅; mobile Lighthouse (throttled 4G, dev box): A11y 96–100 ✅, BP 96 ✅ all four; SEO 100/100 album+academy, 92×2 (button copy only); Perf 96 academy ✅, 64–72 on the three image-heavy pages under simulated slow-4G — the remaining levers (responsive srcset, gallery_tile grids, font deferral, HTTP/2/Cloudflare on prod) are itemized as the straggler list. Fixes shipped in this pass: `text/javascript` added to mod_deflate (the BS bundle was serving uncompressed — 78 KB), `fetchpriority=high` on eager/LCP heroes, iframe title + social-link/Read-more aria-labels (a11y 89→96+). Requests 57→47 (<25 not met: per-partial CSS is a deliberate R3 validity/cacheability trade — documented; HTTP/2 prod makes count moot). Cloudflare = deploy-day option (DEPLOY.md). MilesWeb re-check = post-launch item. | M |
| 35 | ✅ X2 | Backups | mPanel cron: nightly `mysqldump` to a non-web dir + weekly `media/` archive, 14-day retention; documented restore drill (into Docker). Admin "Backups" screen is **status-only** (last-run time, sizes) — no download endpoint (see SEC-20). | cron config, health widget, `admin` backups screen | X1 | Restore drill from a prod dump succeeds in Docker; backup age visible on dashboard. **Done:** `database/backup.sh` (POSIX sh: nightly `--single-transaction --no-tablespaces` gzipped dump + Sunday `media/` tar, 14-day prune, refuses webroot targets, creds read from config/config.php via php — nothing secret in the crontab; mPanel cron line in DEPLOY.md §7); **restore drill performed** — dump → scratch DB → row counts identical (images 478/478, seo_meta 41/41, image_renditions 988/988) → dropped; dashboard **health strip** shows last-backup age+size (status only, no download endpoint per SEC-20); `./backups/` gitignored + mounted into the container so dev matches prod layout. | L |
| 36 | ✅ X3 | Monitoring | UptimeRobot on `/` and `/admin/health.php?token=…`; PHP `error_log` path documented; dashboard health widget (disk, image count, last backup age). | `admin/health.php`, dashboard | X2 | Simulated DB outage → alert within 5 min; widget shows last-backup age. **Done:** health.php extended — `disk_free` (gates <200 MB), `images` count, `sitemap` present, `backup_age` (informational, non-gating like `https`); dashboard health strip (disk/images/backup, red state on failure); **outage drill performed**: db container stopped → health returned 503 on the next request → restarted → 200 (an UptimeRobot 5-min HTTP monitor would alert within one poll); the two exact monitor URLs + error_log location documented in DEPLOY.md §8 (UptimeRobot account setup itself = launch-day user action). | L |

### Stage I — Post-launch owner features (Seq 37–41, added 2026-08-15)

Owner-requested round after the migration completed: visible lockout + a
no-SSH password recovery path, the last hardcoded content areas made dynamic
(testimonial card backgrounds, the gallery hub slider), self-service creation
of academies and albums (with URL/SEO/sitemap handling), and an image
compression/crop audit.

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 37 | ✅ N1 | Lockout UX + password recovery | Login page says "locked, wait N min" after the 5 tries (shadow session counter masks username enumeration in-session — SEC-06 revision); recovery with no SSH/e-mail: one-time token file above the webroot arms `/admin/recover.php` (404 otherwise), self-deletes on success, clears the lock; CLI `database/reset-admin-password.php` for dev. | `admin/login.php`, `admin/recover.php` (new), `database/reset-admin-password.php` (new), SECURITY.md (SEC-06 rev + SEC-24), DEPLOY.md §9, .gitignore | S2/S3 | 5th failure and locked-with-correct-password both show the minutes message; fake username shows the identical message in-session; recover.php 404 when disarmed, full drill passes and file self-deletes (or warns); CLI prints one-time temp password with forced change. **Done:** all of the above verified live against a throwaway `devtest` admin (owner's account untouched); audit rows `login.fail`/`recover.fail`/`recover.ok`/`recover.cli` confirmed; unlink-failure warning path exercised in Docker (uid mismatch) — on mPanel PHP runs as the account user so self-delete works. | M |
| 38 | ✅ N2 | Dynamic testimonial backgrounds | Per-testimonial optional background photo (`testimonials.bg_image_id`, migration 008); NULL → the shipped `card1..3` CSS statics, so default rendering is pixel-identical; editable in panel + overlay. | migration 008, schema, Registry, Repo, testimonial partial, image-ref guards | C3 | Default render byte-identical; setting a photo overrides that card; clearing returns to the static; image delete guarded. **Done:** verified live — visitor markup with NULL is byte-identical (`<div class="card card1">`, no style/data attrs); set image 67 via field API → inline style with the identical gradient+`100% 100%` declarations; **bonus fix: `ensureRendition()` now generates renditions on first use** (legacy → fit per the F2 contract) from the API's image validation, so the chosen photo served an 88 KB rendition instead of the 622 KB `/photos` original; cleared → static returns. Testimonials query is a single JOIN — home stays at exactly 12 queries. Delete-guard + orphan-filter gained the new column. | L |
| 39 | ✅ N3 | Add-academy end to end | `academy` entity creatable/deletable with a validated create-only `slug`; new academies serve at `/academy.php?slug=…` (new generic controller); the 18 legacy slugs keep their files and are delete/rename-protected; auto `seo_meta` row; `sitemap.xml` regenerates. | Registry, `_bootstrap.php` (slug type), item.php guards, `public_html/academy.php` (new), `academy_url()`, co-curriculum grid, section.php, Sitemap writer | C5, F3 | Panel "Add academy" → appears on co-curriculum grid → its page renders with carousel manageable; legacy delete refused; sitemap contains the new URL; slug immutable after create. **Done, verified live end-to-end via the API:** bad slug rejected with a clear message; `roboticsacademy` created → page 200 with its own title/canonical (`$sjSeoSlug`/`$sjCanonicalPath` shell override), grid card links `/academy.php?slug=…`, seo row auto-inserted, sitemap 41→42; duplicate slug → friendly 1062 message; slug edit refused (`create_only`); tamilacademy delete refused (legacy guard); delete → 404 + grid/seo/sitemap all cleaned (image_links cascade); traversal/unknown slugs 404; the 18 legacy pages still 200. Sitemap writes are atomic-with-fallback and non-fatal (result surfaced, never breaks the save). | M |
| 40 | ✅ N4 | Add-album + gallery hub slider | Same creation model for `gallery_album` (+`album_url()`, `public_html/album.php`); the hub's 15-image cross-fade slider becomes `image_links` on a new `pages:gallery` row (seeded from the shipped list — byte-safe), manageable via the panel. | Registry, item.php, `public_html/album.php` (new), gallery view/controller, seed, section.php | C9, N3 | New album creatable with years+photos; legacy albums protected; hub slider order/content editable; default slider = same 15 photos. **Done, verified live:** hub slider renders 15 slides from image_links with byte-identical `url(/photos/…)` values + order (`sportsday1` first at opacity 1) and a static fallback for pre-seed DBs; full album lifecycle — `gal-farewell` created → hub card links `/album.php?slug=…` → page 200 with own title/canonical → year 2026 added → photo attached (renders in `year-2026`) → delete cascades years+links, seo row + sitemap restored to 41; `gal-annual` delete refused (legacy guard); slider reorder via the link API moved slide 1 → hub led with `sportsday10`, restored; panel gains 🖼️ Hub slideshow + ＋ Add album; seeder re-run: "15 slides", idempotent, admin order kept. | M |
| 41.5 | ✅ N6 | Carousel geometry + testimonial bg surfacing | Owner-reported: hero slides of mixed shapes GREW the page when shown (one 3:2 photo among six 16:9) and dragged the caption/button with them; mobile stretched. Fix: every hero carousel (`.carousel-main`, `.abt-carousel` ×3 pages, `.kg-carousel` ×4) + the updates carousel renders imgs at **16:9 with `object-fit: cover`** on desktop (over-tall photos centre-crop, never grow the page; captions stable) and `object-fit: cover` at the shipped fixed mobile sizes (crop, never stretch). `hero_16x7` preset aspect corrected to 16:9/1080 (key kept; upload crop frame now matches the slot; idempotent seed fixup for existing DBs). `ensureRendition` now uses the preset's REAL mode for legacy picks (cover crops — "no outliers"); shipped F2 fit renditions untouched. The three shipped testimonial card backgrounds (CSS-only statics) are now real `bg_image_id` values (fit renditions of the same PNGs, assigned by position only-when-NULL — admin edits win) so the panel shows/edits them; CSS statics remain the fallback. | home/about/achievements/sports/sections css, Pipeline, schema+seed, bootstrap (ASSET_VER 20260815.2) | N2, N5 | **Verified in-browser:** all 7 home slides render exactly 1265×712 (`uniform: true`) — including not-yet-loaded ones (aspect-ratio sizes the box, so image loading can't shift the page); the 3:2 outlier's slide box stays 712; caption at a stable y inside the hero; kg hero 712/712/712; testimonial cards same box (356×922) with `/media/46N/feature_4x3.jpg` + identical gradient/stretch; zero console errors; 17-page sweep 200; seeder idempotent ("3 of 3 assigned", fixups 0 on re-run). **Follow-up (same day): the caption-under-veil bug.** Root cause = BS4→5 drift: the shipped design layers `.carousel-item::before { z-index: 1 }` (dark gradient) over every slide and relied on **Bootstrap 4's built-in `.carousel-caption { z-index: 10 }`** to keep text/buttons above it; BS5 removed that z-index, silently sinking every caption under the overlay since R1. Fixed with the exact BS4 value restored once in tokens.css (site-wide; grep confirms no page sheet competes). Hit-test verified: topmost element at the caption's centre is the caption content on home hero + kg (updates carousel proven by computed order — same context, 10 > 1; the hidden pane can't scroll for a below-fold hit-test). `SJ_ASSET_VER` → 20260815.3. | M |
| 41.7 | ✅ N7 | Admin panel UI v2 | Owner brief: high-class finish, real transitions/button effects, perfect editing controls, **no emojis → professional icons**, live availability/speed/memory view, admin-account management. **Design first** (ADMIN_UI_DESIGN.md v2 spec: principles, tokens, motion, components, screens), then implemented: inline-SVG icon system (`sj_icon()` PHP + `SJUI.icon()` JS — Feather-style, currentColor, CSP-clean; SVG chosen over PNG for DPI-crispness/state-colors/zero requests); `panel.css` rewritten on the v2 tokens (focus rings, hover lift + press on buttons, toggle-switch bools, modal fade+scale, icon-fade row actions, toast redesign, skeleton shimmer, icon-rail responsive collapse, `prefers-reduced-motion`); **dashboard v2** — greeting, live **Site vitals** (`?r=stats` GET endpoint + `dashboard.js` 20 s poll: availability dot, measured round-trip ms + canvas sparkline, server memory bar from /proc/meminfo, PHP peak, disk conic donut, DB+media size, OPcache hit rate, backup age) + icon content cards + **Recent-activity feed** (last 8 audit rows humanised); **Admin Accounts** (owners only): `?r=admins` endpoint (list/create/reset/unlock/role/delete — server-generated one-time temp passwords shown once w/ copy, must_change on first login, self/last-owner guards, all audited `admin.*`), sidebar+screen gated by new `sj_admin_role()` which **re-verifies the account each request** (deleted admin's live session 401s immediately; roles refresh live); `admin_users` stays out of the registry. | ADMIN_UI_DESIGN.md, helpers (sj_icon/sj_admin_role), panel.css, _layout/index/section.php, panel/sj-ui/settings/dashboard.js, api/{stats,admins,index,_bootstrap}.php, login.php, admin-bar | S2, X3 | **Verified live in-browser as a devtest owner:** dashboard "Online · 136 ms · 21.4/30.5 GB · OPcache 98.4%", 59 SVG icons, 8-row feed, **zero emoji in rendered text**, zero console errors; API drill — create/list/dup/bad-username/self-delete/self-role all correct; editor drill — sidebar hides Admins, screen 302s, API 403s, content editing still works; **deleted editor's live session → 401 on next request**; audit rows `admin.create/admin.delete`; icon-name title bug caught+fixed; test accounts removed (owner's account untouched). `SJ_ASSET_VER` → 20260815.4. | M |
| 41.8 | ✅ N7-fix | Idle timeout defeated by the vitals poll | Owner-reported: still signed in after ~10 h. **Root cause (a regression N7 introduced):** `dashboard.js` polls `?r=stats` every 20 s and every request ran `Auth::boot()`'s `last_seen = now`, so an open dashboard tab renewed the session forever — the 30-min idle timeout could never fire and only the 12-h absolute cap ever ended a session. **Fix:** background heartbeats define `SJ_PASSIVE_REQUEST` before the bootstrap (`api/stats.php`, `health.php`); `Auth::boot()` still enforces both timeouts on them but skips the `last_seen` update and id rotation. `dashboard.js` now pauses its interval while `document.hidden`, always polls on load + tab re-focus, and on `401` shows "Signed out" and returns to sign-in. SEC-07 gains the regression note + the standing rule (any new polling endpoint MUST declare the constant). | `src/Admin/Auth.php`, `admin/api/stats.php`, `admin/health.php`, `admin/assets/dashboard.js`, SECURITY.md, ADMIN_UI_DESIGN.md, docs/learn ×2 | N7 | **Measured before/after on a live session:** before — 25 min idle + one poll → clock reset to 0 s (bug reproduced); after — 25 min idle + poll → **clock unchanged at 1500 s**, a real panel page load → resets to 1 s, past 30 min → poll `401` + page `302` + session file destroyed. Browser-verified end to end: vitals live again (Online · 25 ms · 21.9/30.5 GB · OPcache 99.3%), and an expired tab auto-returned to `/admin/login.php`. Regression checks: editing API, token-gated health endpoint and all public pages still 200. **Also caught in this pass:** an initial `document.hidden` guard wrongly skipped the first poll (blank vitals) — corrected so only the repeating timer idles; and the asset version needed a second bump because the JS was edited *after* the first bump (the project's own immutable-cache rule). `SJ_ASSET_VER` → 20260815.6. | M |
| 41.9 | ✅ N8 | Admin panel "Prospectus" redesign | Owner: v2 was "normal professional", not brand-built. Direction chosen with the owner (A — light editorial), fonts self-host approved, auth pages included. Shipped: warm paper canvas + brand fonts genuinely loading for the first time (the CSP had silently blocked Google Fonts — Fjalla One + Dancing Script woff2 now in `assets/fonts/`, OFL note beside); `panel_page_head()` branded header bands (navy icon tile, Fjalla title + gold bar, live stat chips); **`panel_preview_card()` live-preview editors** on every image-led screen — hero slides as 16:9 mini-slides with navy veil + Dancing-Script caption, testimonials as the site's own card recipe (gold Fjalla name, white rule, veil, quote icon), academies/sports/facilities/albums/achievements/unique/updates as site-style card grids — all inside the untouched `.sj-row[data-row]`/`data-list` contract; grid-aware drag (X-midpoint on same visual row, additive); six latent v2 CSS defects fixed (`.sj-form-grid2`, `.sj-save-note`, `input.sj-search`, `img.sj-portrait`, `.sj-modal-head`, picker tabs unstyled); split-panel brand login + brand cards for password/recover; final emoji sweep (picker tabs, used-badge, marks calendar, media upload). `SJ_ASSET_VER` → 20260822.1. | assets/fonts/* (new), panel.css (v3 rewrite), section.php (3 helpers + 19 screens), index.php, login/password/recover, sj-ui.js, panel.js, helpers (calendar icon), ADMIN_UI_DESIGN.md v3, CLAUDE.md font line | N7 | **Verified live in-browser (devtest owner, removed after):** both fonts `document.fonts.check` true; all 19 screens + dashboard 200; hero editor — 7 preview cards, 16:9 boxes, Dancing-Script captions, draggable=true applied, **move persisted through API+reload and was restored**; edit modal opens on preview cards with gold top bar, image field + toggle switch; testimonial cards render the exact site recipe (gold Fjalla name ✔ white rule ✔ veil ✔ quote ✔ 4-line clamp); admins/media/item APIs green; zero console errors; zero emoji; public site untouched (no views/ or public css diffs; home/gallery/kg 200). | M |
| 41 | ✅ N5 | Image compression/crop audit | Every referenced image serves compressed (renditions or `media/static`); `/photos` originals untouched; crops only where the browser already crops (no layout movement); report per-page savings. | backfill.php statics, css urls, gallery slider (via N4) | F2 | No multi-hundred-KB original on any page; hub slider payload drops ≥70%; zero layout movement on unchanged pages. **Done:** full-crawl audit of all 42 URLs found 5 pipeline-bypassing refs + the 15 slider originals. Fixed: academics carousel now uses the existing statics (kg1 1,299→236 KB, highsec1 990→238, high1 324→250), `schname.jpg` (footer bg on EVERY page + about + 2 home bgs) recompressed 5536px→1920 (181→74 KB), slider serves bg_wide fit renditions AND loads lazily (first slide inline, rest applied 2 fades ahead — initial slider payload 3,688→~249 KB, −93%; the shipped page force-downloaded all 15). Honest revert: `band1.jpg` stays original — 1600/q70→238 KB and q58→200 KB both INFLATED its efficient 193 KB source (german.jpg lesson), and it meets the <300 KB bar. Aspect ratios verified equal on all swaps (fit, full frame); all 42 URLs 200; budgets home 12 / gallery 6 / academics 4. **Crop conclusion:** no blanket file-cropping — bg `cover` slots are already browser-cropped (pre-cropping risks framing shifts on 33 shipped pages for marginal bytes) and `<img>` slots proved layout-sensitive in F2; owner-driven crops go through the M3 crop UI, and N2's ensure-on-save generates compressed renditions for every future image pick automatically. `SJ_ASSET_VER` → 20260815.1 (css changed). | M |

### Stage J — Public "Global Campus" redesign (Seq 42–48, added 2026-08-23)

Owner-commissioned full visual redesign of the public site (the admin panel keeps
its N8 "Prospectus" design). Direction chosen with the owner via style tiles:
**A — Global Campus** (white canvas, navy anchors, gold precision accents, Fjalla
One display + Manrope body, Dancing Script sparse); depth = all 42 pages
re-skinned + Home/shell rearchitected; ALL signature elements (stat counters,
floating enquiry CTA, mega menu, Ken Burns); staged shell-first rollout. The
spec is `PUBLIC_UI_DESIGN.md`; the CLAUDE.md visual freeze was retired in J0
(its precondition — migration + admin workflow complete — was met, and the
redesign request is the owner approval the rule required).

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 42 | ✅ J0 | Design system + governance | `PUBLIC_UI_DESIGN.md` (palette, type, space, motion, components, hard constraints, rollout map); CLAUDE.md visual-freeze → visual-fidelity rule (spec-fidelity for migrated pages, no-drift for the rest, admin-lockstep, immutable geometry); this Stage J table. Docs only. | PUBLIC_UI_DESIGN.md (new), CLAUDE.md, PHASES.md | N8 | Docs-only diff; no rendered change. | L |
| 43 | ✅ J1 | Foundation: fonts + tokens v2 + site.css + motion runtime | shell.php fonts → Fjalla One + Manrope + Dancing Script (League Spartan/Raleway out — most "body" text already rendered system sans via the `"LeagueSpartan"` typo, so the visible delta is modest); tokens.css v2 (paper/neutrals/gold-soft, type scale, section pad, radii, shadows, `--sj-veil`, body font → Manrope); NEW css/site.css (base type, section bands, `.sj-sec-head` gold bar, unified `.sj-btn`, `.sj-card`, reveal states, Ken Burns keyframes, shared hero geometry, smooth scroll, GLOBAL `prefers-reduced-motion` kill-switch — the public site had none); site.js v2 (`sjMotionOK`/`sjRevealIO` one-time IO reveals/`sjCounters`/`sjNavScroll`; legacy `sjReveal`/`sjBlurCards` kept until J6). | views/shell.php, css/tokens.css, css/site.css (new), css/partials/navbar.css (interim font var), js/site.js, bootstrap.php (ASSET_VER) | J0 | All 42 URLs 200; `document.fonts.check('1em Manrope')` true; no League Spartan/Raleway requests; console clean; home stays 12 queries; reduced-motion emulation kills smooth scroll. | M |
| 44 | ✅ J2 | Shell chrome: mega menu + sticky nav + footer + admissions band + floating CTA | navbar.php rewrite — fixed-top, Bootstrap Dropdown mega panels (static hrefs, zero queries: Home · About · Academics · Student Life · Gallery), Offcanvas mobile, hover-intent ≥992px, `$sjNavOverlay` transparent→solid, inline sticky script → `sjNavScroll()`; footer re-skin (navy-dark, gold hairline, same 4 columns/settings); jumbotron → Admissions band v2; NEW floating-cta.php in shell (Admissions pill + WhatsApp when new `whatsapp_number` setting non-empty — new key because the seeded contact_phone is a landline; blank hides the button); scroll-up → bottom-left; settings plumbing (seed + admin group + api whitelist w/ allow-empty). | views/partials/{navbar,footer,jumbotron,floating-cta(new),scroll-up}.php, css/{site,footer}.css, css/partials/{jumbotron,scroll-up}.css, js/site.js, views/shell.php, database/seed.php, admin/section.php, admin/api/settings.php, bootstrap.php | J1 | Mega panels: click/hover/Esc/tab; offcanvas at 375px; FAB + scroll-up no overlap; wa.me link correct from admin-set number, hidden when blank; queries unchanged; SECURITY §4; seeder idempotent. | M |
| 45 | ✅ J3 | Home rearchitecture: hero partial + stat band + de-triplication | NEW views/partials/hero.php (variants `hero`/`strip`; N6 16:9 geometry + `--sj-veil` scrim + Ken Burns in site.css ONCE; ed_* hooks; img_tag contract verbatim); carousel.php → thin adapter; home.php stat band from 8 new settings (`home_stat{1..4}_value/_label`), inline counter script → `sjCounters()`; home-bs4-remnants link removed; testimonial veil single-sourced (`--sj-tm-bg` custom property, inline style shrinks); home.css token rewrite + ticker/new-updates/marks-scroll/contact re-skins; **admin lockstep**: panel.css `--veil`/`.sj-prev-cap-*`/`.sj-prev-tm*` mirror the new recipes. | views/partials/{hero(new),carousel,testimonial,update-scroll,new-updates,marks-scroll,contact}.php, views/pages/home.php, css/home.css + partial css, admin/assets/panel.css, database/seed.php, admin/section.php, admin/api/settings.php, js/site.js, bootstrap.php | J2 | Home EXACTLY 12 queries; stat values editable + round-trip; counters once w/ suffixes; slide boxes uniform incl. unloaded; caption hit-test above scrim; admin previews match; edit-overlay works on hero/testimonials; 375px pass; SECURITY §4. | M |
| 46 | ✅ J4 | Generic families (~34 URLs) | section.php/academy.php/album.php adopt hero.php (top `hero`, mid `strip`); grids/headings → `.sj-card`/`.sj-sec-head`; buttons → `.sj-btn`; token rewrites of sections/academy/album css; `$sjHeadCss` + ed_* hooks kept. | views/pages/{section,academy,album}.php, css/{sections,academy,album}.css, bootstrap.php | J3 | kg + tamilacademy + a slug academy + gal-annual + a slug album browser-verified; 34-URL curl sweep 200; budgets unchanged; overlay editing works. | M |
| 47 | ✅ J5 | Bespoke hub pages | about/staffs/academics/infrastructure/achievements/sports/co-curriculum/gallery: hand-rolled heroes → hero.php; card grids → `.sj-card` (blur-siblings retired, per-page JS emptied); gallery keeps cross-fade + data-bg lazy (re-skin only); token rewrites of the 8 sheets + groups/gallery-slider/preloader css; `$sjNavOverlay` on hero pages; about.php inline gold → token. | views/pages/×8, css ×8 + partials, js per-page, bootstrap.php | J4 | Each page desktop+375px pass; keyboard focus on cards; gallery initial payload still one image; budgets unchanged; console clean. | M |
| 48 | ✅ J6 | Sweep + audits + docs truth pass | Retire dead files (home-bs4-remnants.css, partials/navbar.css, emptied JS, sjReveal/sjBlurCards at zero callers); greps: `#2b4b8a` outside tokens ≈0, League Spartan/Raleway 0, dupe `:root` 0; a11y (contrast, aria, focus), full reduced-motion pass, Lighthouse vs docs/perf-baseline.md; WEBSITE_CONTEXT.md truth pass, docs/learn/11-stage-j.md, final CLAUDE/PHASES/PUBLIC_UI_DESIGN wording. | deletions, docs | J5 | Audits pass; Lighthouse ≥ F4 scores; docs true. | L |

### Stage K — Global Campus refinement + admin editor UX (Seq 49–51, added 2026-08-23)

Owner feedback on Stage J: cards/testimonials/gallery approved; the script
font, navbar and full-screen hero carousels felt "childish" and buried the
page content; the mobile marks marquee was crushed; and the admin rich-text
toolbar lacked toggle states (gold/italic irreversible in place), modals had
no Esc, the testimonial preview was covered by a giant quote icon, and full
sections still offered "Add". Decisions made with the owner: Fjalla-only
bold type, two-tier international navbar, compact Home hero + slim inner
banners, stacked marks columns on mobile, testimonial cap = 3.

| Seq | ID | Name | Goal | Files touched | Depends | Acceptance | Risk |
|---|---|---|---|---|---|---|---|
| 49 | ✅ K1 | Type purge + two-tier navbar + hero v2 + marks mobile | Dancing Script OUT of the public site (accents → gold Fjalla / uppercase Manrope); navy utility strip (settings phone/email + gold CTA) over a white bar with UPPERCASE links and an active-page-only gold underline (`.sj-navhead` wrapper, server-side SCRIPT_NAME actives); Home hero capped `min(56.25vw, 62vh)` + scroll cue; ALL inner pages → slim `banner` hero variant `clamp(240px,36vh,400px)` (all slides still cross-fade, no Ken Burns/controls); marks year-columns stack full-width ≤900px at readable sizes; panel.css preview sub-line lockstep. | shell.php, tokens/site/6-page css, navbar/hero + 8 page templates, marks-scroll.css, panel.css, bootstrap (VER .8) | J6 | **Verified live:** fonts request = Fjalla+Manrope, 0 Dancing requests/faces; hero 446px on 720px viewport with cue; strip renders phone/email/CTA; links uppercase 1.2px LS; Home underlined on /, Academics group + Kindergarten item active on /kg.php; kg banner slim (clamp floor in the vh-less dev pane), indicators hidden, Ken Burns off; marks stacked 3×297px at 375px; sweep 200, home 12 queries, console clean. | M |
| 50 | ✅ K2 | Admin editor UX | Toolbar v2 (shared by panel + overlay): selectionchange state engine lights B/I (queryCommandState) and Gold (hl-gold ancestry) with `.on`; Gold toggles OFF via extracted unGold(); '✕ fmt' → separated 'Clear' action. Esc closes the topmost panel modal/picker (openModal keydown + onClose hook → pickImage cancels). Preview quote out-specifies the media-img cover rule (32px corner badge again). Registry `max_count` (testimonial 3): both Add affordances hide at the cap (panel shows a "Section full" chip) + authoritative count check in item.php create. | sj-ui.js, panel.css, css/admin.css, Registry, EditAttrs/helpers, admin/_layout + section.php, api/item.php, testimonial partial, bootstrap (VER .9) | K1 | **Verified live (devtest owner, removed after):** 8/8 toolbar drill (gold off→apply→ON inside→click removes→off; bold ON/off with live state; Esc closed the modal); toolbar = B/I/Gold/│/Clear; quote 32×32; Add absent at 3 + full chip; 4th create → friendly "section is full" error, count stayed 3; overlay add-hook also hidden; SECURITY §4 PASS (registry-literal identifiers, bound parent value). | M |
| 51 | ✅ K3 | Docs truth pass | PUBLIC_UI_DESIGN v1.1 (type §2, hero variants + two-tier navbar §5, max_count constraint), CLAUDE.md fonts line, this table; final greps (public Dancing Script consumers = 0). | docs | K2 | Greps clean; docs true. | L |
| 53 | ✅ K7 | Owner batch: prefill + 3 dynamic areas + gold toggle | Add forms open PRE-FILLED with the previous row's text/url/enum values (panel_add_attr/ed_add `last` param, wired on hero/updates/toppers/timeline/events/timings/staff-block adds); Home motto → 6 settings; Staffs blocks → creatable `staff_blocks` entity (migration 010, shipped pair copied once, auto left/right alternation any count); Academics hero → pages row + 3 seeded slides, edited in an 'Academics page' area ABOVE the School Sections tabs; per-academy `fancy_gold` switch neutralizes the legacy large-gold inline-styled spans; gold parity verified (toolbar = site = #8a6d00 ink on white; navy-band .hl-gold now renders #ffd700); justify sweep. | migration 010 + schema + seed, Registry/Repo/helpers/EditAttrs, panel.js/admin.js, section+_layout+settings api, card/staffs/academics/academy/about/section/hero/new-updates views, academy/home/staffs css, bootstrap (VER 20260825.1) | K6 | **Verified live:** seeder idempotent; staffs 200@9q both blocks; academics 200@6q dynamic hero (0 static refs); fancy_gold API round-trip flips .sj-hl-off; hero Add carries last:{caption_title…}; all three admin areas render; 9-page sweep 200; SECURITY §4 PASS. | M |
| 52 | ✅ K4 | Owner polish round | Scroll cue fades after 60px; About rules block fully dynamic — `rules_timings` table (migration 009, dev-guarded seeder CREATE + 5 shipped rows empty-only), registry entity + About-Page admin list + overlay hooks, diary line/URL → settings (`diary_text`/`diary_url`); nav: active-underline moved to ::before (Bootstrap's ::after CARET survives on active groups), last two panels open right-aligned (no viewport overflow), UA focus box replaced by a gold :focus-visible ring, hover engine closes all sibling panels INSTANTLY on enter (no overlap on fast sweeps). | site.js/site.css, migration 009 + schema + seed, Registry/Repo/helpers, about controller+view, admin section + settings api, bootstrap (VER .10) | K3 | **Verified live:** about = 5 DB rows + settings diary link at 10 queries; seeder '5 seeded'→'kept'; About active WITH caret; one panel max after switching; sweep 200, console clean. Hover feel needs a real mouse (dev pane reports pointer:coarse — engine correctly off there); SECURITY §4 PASS. | M |

---

## 3. Performance Plan (Stage D/H detail)

The "48% healthy, very slow" report is dominated by: unoptimized full-size photos (478 files, many >1 MB), `?v=time()` defeating all CSS caching, 4 Bootstrap CDN versions, nested-document HTML, and no compression headers. Measured order of attack:

1. **F0 baseline first** — no optimization without a before-number. Re-measure at F1, F2, F4.
2. **Kill `?v=time()` (F1, biggest cache win).** Replace with `?v=SJ_ASSET_VER` (constant bumped per deploy). Repeat-visit CSS/JS requests drop to zero.
3. **.htaccess (F1):** `mod_deflate` on text (~70% transfer cut); `mod_expires`/`Cache-Control` immutable for images/versioned assets, `no-cache` for HTML; `Options -Indexes`.
4. **One Bootstrap (R1a–c):** self-hosted 5.3.3 replaces 4 CDN versions → 3 fewer render-blocking downloads, 2 fewer DNS connections; fonts via a single `display=swap` request with preconnect (or self-hosted).
5. **Rendition backfill (F2), the heavy lifter:** generate in Docker, upload results. Expected: Home image payload from the ~8–12 MB class to <1.5 MB; mobile LCP from >8 s to <3 s.
6. **Query budget (F0 rule):** ≤ 12 queries/page; `settings` and `pages` row cached per request; no N+1 in loops (repos return image data pre-joined).
7. **OPcache (F1/X1):** confirm via `admin/health.php`; set `opcache.validate_timestamps=1, revalidate_freq=60` if mPanel exposes PHP settings.
8. **SEO (F3)** and **Cloudflare (F4, optional):** free tier absorbs the shared host's slow static serving; verify `/admin*` bypass.
9. **Targets (F4):** mobile Lighthouse Perf ≥ 80 / SEO ≥ 95 / BP ≥ 90 / A11y ≥ 90; Home fully-loaded < 3 MB, < 25 requests.

---

## 4. Deploy Story (Docker dev → mPanel prod)

- **Parity:** Docker mounts the repo root; `public_html/` is the docroot in both. Same PHP 8.3, GD, PDO. `config/config.php` differs per environment and is created by hand once in each — never transferred through git or bundled in the zip.
- **Release procedure:** (1) tag; (2) build a release zip locally from the whitelist: `public_html/`, `src/`, `views/`, `vendor/`, `config/config.sample.php`; (3) upload via mPanel file manager / FTP, extract over the previous release; (4) apply `database/migrations/*.sql` via mPanel's DB tool (**additive-only** — no destructive DDL); (5) bump `SJ_ASSET_VER`; (6) open `/admin/health.php` → all green; (7) smoke list: Home, one page per family, admin login, one edit round-trip.
- **Never upload:** `database/` (schema/seed/backfill are dev-only; seed prints credentials), `.git/`, `docker-compose.yml`/`Dockerfile`/`run.sh`, dev `config/config.php`, `*.md` plans, dumps/archives (root `.htaccess` denies them anyway — SEC-12/20).
- **Data direction rule:** content flows dev→prod only until X1; after that **prod DB is the source of truth** — pull nightly dumps (X2) into dev for testing, never push a dev DB over prod.
- **Rollback:** keep the two previous release zips in a non-web `releases/` dir; rollback = extract previous zip (additive migrations mean old code runs on new schema).

---

## 5. Working agreements

- Ship to prod after every phase from X1 (Seq 14) onward.
- Every phase closes with: acceptance criteria demonstrated, `SECURITY.md` §4 checklist affirmed (per `CLAUDE.md`), `WEBSITE_CONTEXT.md` updated if page structure changed.
- Multi-user/roles: `role` exists from S2; when a second user is actually needed, add registry permission flags + per-entity checks in `_bootstrap.php` (SEC-09) and enable the "Users & roles" card (ADMIN_UI_DESIGN screen 22).
